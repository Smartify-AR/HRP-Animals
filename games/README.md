# Games

Add game folders here. Each game should have an `index.html` entry point.

Link to games from the Games page (`pages/games.html`) or from `data/assets.json` (e.g. `gamePath` in gameBadges). See Documentation.md for how to add and link games.
